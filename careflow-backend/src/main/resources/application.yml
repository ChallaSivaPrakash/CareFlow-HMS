spring: 
  datasource: 
    url: jdbc:mysql://localhost:3306/hospital_db?allowPublicKeyRetrieval=true&useSSL=false 
    username: root 
    password: Challa@1206 
  jpa: 
    hibernate: 
      ddl-auto: update 
    show-sql: true 
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: challasivaprakash6@gmail.com
    password: abcdefghijklmnop
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  

app: 
  jwt: 
    secret: ${JWT_SECRET:7c6f6c75656e7465727072697365323032357365637265746b6579666f7268616d617070} 
    expiration-ms: 1800000     # 30 minutes
    refresh:
      expiration-ms: 604800000 # 7 days
  triage: 
    mode: heuristic 
    engine: llm        # Options: heuristic, llm. Set to "heuristic" to disable AI. 
    human-in-loop: true # Always true when engine=llm 

websocket: 
  broker: 
    type: simple      # Options: simple, redis. Use "redis" for production. 
    host: localhost 
    port: 6379 
 
payment: 
  provider: stripe   # Options: stripe, razorpay 
  stripe: 
    secret-key: ${STRIPE_SECRET_KEY} 
    webhook-secret: ${STRIPE_WEBHOOK_SECRET} 

server: 
  port: 8080 

encryption: 
  secret-key: ${ENCRYPTION_SECRET_KEY:WJ6dLSPFk/G7GYrMzP0o5w==}  # 256-bit AES key in Base64; override via env 
